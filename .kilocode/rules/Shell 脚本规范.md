# 运维脚本编写规范

所有生成的 `.sh` 脚本必须满足以下要求：

- **兼容性**：必须兼容 Debian 11/12 和 Ubuntu 22.04+。
- **非交互式**：调用 `apt` 时必须附带 `-y` 参数，并设置 `DEBIAN_FRONTEND=noninteractive`。
- **安全性**：在执行 `rm` 或 `chmod` 之前，必须先检查目标路径是否存在。
- **头部声明**：必须包含 `set -e` 以确保脚本在出错时立即停止。

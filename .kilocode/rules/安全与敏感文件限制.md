# 安全控制与隐私保护

为了防止敏感信息泄露，AI 必须严格遵守以下限制：

## 1. 限制访问的文件

严禁读取或在回复中输出以下文件的内容：

- `.env`
- `**/config.toml` (若包含真实 Token)
- `*.key`
- `credentials.json`

## 2. 敏感信息处理

- 在生成示例代码时，必须使用占位符：`your_bot_token_here` 或 `123456789:ABC...`。
- 严禁将真实的 `admin_chat_id` 硬编码在任何 `.go` 或 `.rs` 文件中。

# 多语言实现一致性

本项目同时维护 Go 和 Rust 两个版本，AI 在修改功能时需注意：

- **命令同步**：若在 Go 版本中增加了新命令（如 `/reboot`），必须提醒用户在 Rust 版本的 `handlers.rs` 中同步实现。
- **逻辑对齐**：系统状态（CPU/内存）的计算逻辑必须保持一致，确保两个版本输出的报表格式相同。
- **依赖选型**：
  - Go 版本优先使用 `tgbotapi`。
  - Rust 版本优先使用 `teloxide`。
